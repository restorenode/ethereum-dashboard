import React from "react";
import styles from "./PageTitle.module.scss";

const PageTitle = () => {
  return <h1 className={styles.title}>Ethereum Insights Dashboard</h1>;
};

export default PageTitle;
